import React from 'react';

const PermissionDenied = () => (
  <div style={{ textAlign: 'center', marginTop: '50px' }}>
    <h2>You don't have the necessary permissions for this operation.</h2>
  </div>
);

export default PermissionDenied;
